# Fichier : environment.yml
name: hml_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - numpy>=1.26.0
  - open3d>=0.18.0
  - scipy>=1.13.0
  - flask>=3.0.0
  - werkzeug>=3.0.0
  - pip
  - pip:
    - rosbags>=0.9.15
